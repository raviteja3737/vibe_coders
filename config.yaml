# Training Configuration

# Image settings
image_width: 518
image_height: 294

# Training hyperparameters
batch_size: 4
epochs: 50
lr: 0.0001
weight_decay: 0.01

# Model settings
num_classes: 10
backbone: "dinov2_vitb14_reg"

# Early stopping
patience: 10
min_delta: 0.001

# DataLoader settings
num_workers: 4
pin_memory: true
